# FS-180-HDS-010-SDS-010-SMS-040 SMT

Row-local source for the mini bidirectional nft rule generation SMT.

Run:

```bash
tests/run-active-lab-mini-smt.sh bidirectional-nft
```

This row may start only `router-a` and `router-b`.
