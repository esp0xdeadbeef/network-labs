# FS-190-HDS-010-SDS-010-SMS-010 SMT

Row-local source for the mini service exposure classification SMT.

Run:

```bash
tests/run-active-lab-mini-smt.sh service-exposure-classification
```

This row may start only `access-node` and `core-node`.
