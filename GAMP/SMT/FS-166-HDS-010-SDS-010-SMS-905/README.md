# SMT Construction Row: FS-166-HDS-010-SDS-010-SMS-905

Status: NOT OK overall. Construction is OK; fresh cold-stage runtime evidence
is not yet recorded.

The validation scheme constructs a conformant CPM replacement artifact for
exactly `wireguard-egress` from the row-owned scenario definition. It executes the controlled skip
acknowledgements, realization, schema release, one normalized WireGuard
platform-binding bundle, and the WireGuard canonical adapter. Secret
delivery stays opaque and carries no key material in evaluation output.

```bash
bash tests/FS-166-HDS-010-SDS-010-SMS-905.sh
```

No direct-CPM runner, renderer sidecar, or legacy source is part of this row.
