# FS-310-HDS-020-SDS-010-SMS-200 SMT

Row-local source stub for renderer bridge-network no-default contract.

Construction-only — no active-lab mini-SMT runtime targets.
Governing SMS: renderer bridge-network and VM-network modules must not supply hardcoded bridge addresses, DHCP server enablement, IP masquerade behavior, DHCP pool offsets, or address ranges when CPM data is missing.

Construction test in owning repo: network-renderer-containerlab-linux-backend.
