# FS-500-HDS-010-SDS-010-SMS-010

SMS template row for the `reachability-decision` input.

The source is a small row-local `intent.nix` with one relation and two runtime
targets. It is intended for focused deterministic POC tests, not HAT/SAT.
