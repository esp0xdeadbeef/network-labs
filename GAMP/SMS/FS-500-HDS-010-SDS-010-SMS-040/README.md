# FS-500-HDS-010-SDS-010-SMS-040

SMS template row for the `p2p-next-hop` input.

The source is a small row-local `intent.nix` with two router targets and one p2p
route atom. It is intended for focused deterministic POC tests, not HAT/SAT.
